import 'package:flutter/material.dart';

class OverviewCardMediumScreenSizes extends StatelessWidget {
  const OverviewCardMediumScreenSizes({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}